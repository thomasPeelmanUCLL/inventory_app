apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: inventory
data:
  NODE_ENV: "production"
  PORT: "3000"
  # FRONTEND_URL and BACKEND_URL are injected via secret, not here
  # DATABASE_URL is injected via secret, not here
